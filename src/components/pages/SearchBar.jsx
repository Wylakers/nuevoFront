import React from "react";
import Layout from "../layout";
import SearchBar from "../Historias/SearchBar/SearchBar";

const Search = () => {
  return (
    <>
      <Layout main={<SearchBar />} />
    </>
  );
};

export default Search;
