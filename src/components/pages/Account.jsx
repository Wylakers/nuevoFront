import React from "react";
import Layout from "../layout";
import AccountInfo from "../Historias/AccountInfo";

const Account = () => {
  return (
    <>
      <Layout main={<AccountInfo />} />
    </>
  );
};

export default Account;
